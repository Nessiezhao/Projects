状态码高速浏览器为什么出错
1开头表示正在处理
2开头请求正常处理完毕
3开头需要附加操作完成请求
4开头客户端出错. 比如客户端发来的http有错误
5开头服务器处理请求错误
200正常
204请求的文件是一个空文件
206断点续传(请求一半断开了, 但是等会有请求)
301永久性重定向(将之前老的url保存为书签, 使用新的url请求)(菜馆)
302临时性重定向
307
403服务拒绝(访问的资源被服务器拒绝了)
404资源未找到
503服务器繁忙稍后再试
mysql底层用的是TCP, 绑定的端口号是3306, 可以跨网络服务


host是主机, user是用户(root), passwd是密码, 数据库, 端口号, 后面的两个为空
连接如果失败返回的是空


